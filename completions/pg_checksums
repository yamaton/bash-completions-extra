# Auto-generated with h2o

_pgchecksums()
{
    local cur prev words cword
    _init_completion -s || return

    local word_list="  -c --check -d --disable -e --enable -f --filenode -N --no-sync -P --progress -v --verbose -V --version -? --help"
    COMPREPLY=( $(compgen -W "${word_list}" -- "$cur") )
}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _pgchecksums pg_checksums
