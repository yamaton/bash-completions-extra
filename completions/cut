# Auto-generated with h2o

_cut()
{
    local cur prev words cword
    _init_completion -s || return

    local word_list="  -b --bytes -c --characters -d --delimiter -f --fields -n --complement -s --only-delimited --output-delimiter -z --zero-terminated --help --version"
    COMPREPLY=( $(compgen -W "${word_list}" -- "$cur") )
}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _cut cut
