# Auto-generated with h2o

_gstlaunch10()
{
    local cur prev words cword
    _init_completion -s || return

    local word_list="  -h --help --help-all --help-gst -t --tags -c --toc -v --verbose -q --quiet -m --messages -X --exclude -f --no-fault -e --eos-on-shutdown --version --no-position --force-position"
    COMPREPLY=( $(compgen -W "${word_list}" -- "$cur") )
}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _gstlaunch10 gst-launch-1.0
