# Auto-generated with h2o

_pbzip2()
{
    local cur prev words cword
    _init_completion -s || return

    local word_list="  -1 -2 -3 -4 -5 -6 -7 -8 -9 -b -c --stdout -d --decompress -f --force -h --help -k --keep -l --loadavg -m -p -q --quiet -r --read -S -t --test -v --verbose -V --version -z --compress"
    COMPREPLY=( $(compgen -W "${word_list}" -- "$cur") )
}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _pbzip2 pbzip2
